% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_CP.R
\name{get_CP}
\alias{get_CP}
\title{Conditional power at interim (Eq. 2.10)}
\usage{
get_CP(timing, n, mean_cohen_d, K, sum_rho, alloc_rate = 1)
}
\arguments{
\item{timing}{Numeric in (0, 1). Interim information fraction.}

\item{n}{Numeric, planned final \strong{control group} sample size (before SSR).}

\item{mean_cohen_d}{Numeric, observed mean Cohen's d at interim
(i.e. \eqn{\bar d_1}).}

\item{K}{Integer, number of endpoints.}

\item{sum_rho}{Numeric, sum of all off-diagonal interim correlations
\eqn{\sum_{p \ne q} \hat\rho_{pq,1}}.}

\item{alloc_rate}{Numeric > 0, treatment:control allocation ratio. Default \code{1}.}
}
\value{
Numeric scalar: the conditional power.
}
\description{
Conditional power at interim (Eq. 2.10)
}
\details{
This function calls \code{get_efficacy_boundaries(timing)} and takes the
final-stage boundary \eqn{z_{\alpha_2}}
}
\examples{
# 50\% look, planned 80 controls total, 1:1 allocation, 6 endpoints
get_CP(
  timing = 0.5,
  n = 80,
  mean_cohen_d = 0.3,
  K = 6,
  sum_rho = 9,
  alloc_rate = 1
)

}
