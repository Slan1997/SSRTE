% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_example_one_stage_data.R
\name{simulate_example_one_stage_data}
\alias{simulate_example_one_stage_data}
\title{Simulate one-stage multivariate continuous endpoint data}
\usage{
simulate_example_one_stage_data(
  n_trt,
  n_ct,
  n_endpts,
  exp_mean_cohen_d,
  mu_ct,
  sd,
  rho,
  seed
)
}
\arguments{
\item{n_trt}{Integer. Number of subjects in the treatment group (one-stage).}

\item{n_ct}{Integer. Number of subjects in the control group (one-stage).}

\item{n_endpts}{Integer. Number of endpoints.}

\item{exp_mean_cohen_d}{Numeric scalar. Expected mean Cohen’s d across all endpoints.
The mean shift is computed as \code{exp_mean_cohen_d * sd}.}

\item{mu_ct}{Numeric vector of length \code{n_endpts}. Mean vector for the control group.}

\item{sd}{Numeric vector of length \code{n_endpts}. Standard deviations for each endpoint.}

\item{rho}{Numeric \code{n_endpts x n_endpts} correlation matrix.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A list with two matrices:
\describe{
\item{y_trt}{Matrix of treatment group data (\code{n_trt x n_endpts}).}
\item{y_ct}{Matrix of control group data (\code{n_ct x n_endpts}).}
}
}
\description{
This function generates simulated treatment and control data
with the \strong{same covariance structure} but different means based on
a target (expected) Cohen’s d value.
}
\details{
The covariance matrix is constructed as:
\deqn{\Sigma = \text{diag}(sd) \, \rho \, \text{diag}(sd)}

The treatment mean vector is computed as:
\deqn{\mu_{\text{trt}} = \mu_{\text{ct}} + (\text{exp\_mean\_cohen\_d} \times sd)}
}
\examples{
  rho <- matrix(c(
    1.0, 0.1, 0.3, 0.7, 0.1, 0.3,
    0.1, 1.0, 0.7, 0.1, 0.3, 0.7,
    0.3, 0.7, 1.0, 0.1, 0.3, 0.7,
    0.7, 0.1, 0.1, 1.0, 0.1, 0.3,
    0.1, 0.3, 0.3, 0.1, 1.0, 0.7,
    0.3, 0.7, 0.7, 0.3, 0.7, 1.0
  ), nrow = 6, byrow = TRUE)

  sim <- simulate_example_one_stage_data(
    n_trt = 40, n_ct = 40,
    n_endpts = 6,
    exp_mean_cohen_d = 0.3,
    mu_ct = c(4, 6, 4, 5, 7, 6),
    sd = c(0.5, 0.5, 1, 1, 2, 2),
    rho = rho,
    seed = 15
  )
  str(sim)

}
