% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_efficacy_boundaries.R
\name{get_efficacy_boundaries}
\alias{get_efficacy_boundaries}
\title{Get one-sided Lan-DeMets O'Brien-Fleming efficacy boundaries (2-look design)}
\usage{
get_efficacy_boundaries(timing, alpha0 = 0.025, sfu = sfLDOF)
}
\arguments{
\item{timing}{Numeric scalar in \code{(0, 1)} giving the \strong{interim}
information fraction (e.g. \code{0.5} for 50 percent information at interim).
The final look is taken to be at 1.}

\item{alpha0}{One-sided type I error to spend. Default \code{0.025}.}

\item{sfu}{Upper (efficacy) spending function. Default
\code{gsDesign::sfLDOF}, which gives a Lan-DeMets O'Brien-Fleming–type
spending.}
}
\value{
A numeric vector of length 2: \code{c(C1, C2)}, where
\code{C1} is the interim efficacy boundary and \code{C2} is the final
efficacy boundary (both on the Z-scale).
}
\description{
Compute one-sided group-sequential \strong{efficacy} boundaries for a
2-stage (1 interim + final) trial using the Lan-DeMets
O'Brien-Fleming-type alpha-spending function from \pkg{gsDesign}.
}
\details{
This is a small convenience wrapper around \code{gsDesign()} for the
very common case where one has \strong{one interim at time t} and then
the final at time 1.
}
\examples{
efficacy_bounds <- get_efficacy_boundaries(timing = 0.5)
efficacy_bounds

}
\seealso{
\code{\link[gsDesign]{gsDesign}}, \code{\link[gsDesign]{sfLDOF}}
}
